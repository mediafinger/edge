- if @profile

  %h2
    Your user profile #{@profile.name}

  = simple_form_for(@profile, :html => {:class => 'form-horizontal' }) do |f|
    = f.label :email
    = @profile.user.email

- else
  You got no profile!