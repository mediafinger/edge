/ ### Begin of partial shared/_nav.html.haml ###

.navbar.navbar-fixed-top
  .navbar-inner
    .container-fluid
      %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.brand{:href => root_path}Edge
      .container-fluid.nav-collapse
       
        - if user_signed_in?
          %ul.nav
            %li.font-xl= link_to iconic_nav(:home), home_path, :title => t('shared.nav.home')
            %li.font-xl= link_to iconic_nav(:star), events_path
            %li.font-xl= link_to iconic_nav(:cog), profile_path(current_user.profile)
            %li.font-xl= link_to iconic_nav(:user), profiles_path
            %li.font-xl= link_to iconic_nav(:rss_alt), be_social_path
            %li.font-xl= link_to iconic_nav(:undo), destroy_user_session_path, :method => :delete, :title => t('shared.nav.sign_out')

          - else
            %ul.nav
              %li.font-xl= link_to iconic_nav(:fullscreen_exit_alt), new_user_registration_path, :title => "Sign up now!"
