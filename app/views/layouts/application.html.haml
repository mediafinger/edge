!!! 5
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %title= content_for?(:title) ? yield(:title) : "Edge on Air"
    = csrf_meta_tags
    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
      = javascript_include_tag "http://html5shim.googlecode.com/svn/trunk/html5.js"
    / Le styles
    :css
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    = stylesheet_link_tag "application", :media => "all"
    / Le fav and touch icons
    / %link{:href => "images/favicon.ico", :rel => "shortcut icon"}/
    %link{:href => "images/apple-touch-icon.png", :rel => "apple-touch-icon"}/
    %link{:href => "images/apple-touch-icon-72x72.png", :rel => "apple-touch-icon", :sizes => "72x72"}/
    %link{:href => "images/apple-touch-icon-114x114.png", :rel => "apple-touch-icon", :sizes => "114x114"}/


  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container-fluid
          %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.brand{:href => "#"}Edge
          .container-fluid.nav-collapse
            - if user_signed_in?
              %ul.nav
                %li= link_to image_tag("iconics/settings.png", :class => "icon", :alt => "settings"),  edit_user_registration_path
                %li= link_to image_tag("iconics/user_24x.png", :class => "icon", :alt => "profile"),   profile_path(current_user.profile)
            - else
              %ul.nav
                %li= link_to image_tag("iconics/invite.png", :class => "icon", :alt => "register!"),   new_user_registration_path

    .container-fluid

      .row-fluid
        .span3
          .well.sidebar-nav
            %ul.nav.nav-list

              - if user_signed_in?
                %li.nav-header= t('shared.nav.signed_in_as')
                %li= current_user.email
                %li= link_to_twitter current_user
                %li= profile_img current_user
                %li= link_to_website current_user
                %li= link_to image_tag("iconics/key.png", :class => "icon", :alt => t('shared.nav.sign_out')), destroy_user_session_path, :method => :delete
                %li= link_to image_tag("iconics/link.png", :class => "icon", :alt => t('shared.nav.linked_authentication_services')), authentications_url
              -else
                %li.nav-header= t('.sign_in')
                / Sign in with username & password
                %li= link_to image_tag("iconics/key.png", :class => "icon",  :alt => t('shared.nav.sign_in')), new_user_session_path
                / Sign in using an Oauth provider
                -['Twitter', 'Google', 'Github', 'Facebook', 'Open_ID', 'XING' ].each do |provider|
                  %li= link_to(image_tag("auth/#{provider.downcase}_32.png", :class => 'gfx', :alt => "#{provider}"), "/auth/#{provider.downcase}")

        .span9
          - flash.each do |key, value|
            = content_tag(:div, value, :class => "round flash #{key}")
          
          %main
            = yield
    

    /
      Le javascript
      \==================================================
    / Placed at the end of the document so the pages load faster
    = javascript_include_tag "application"
